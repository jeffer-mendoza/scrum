{% extends 'base.html.twig' %}

{% block body %}
    <h1>BusinessProcesses edit</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a class="btn btn-default" href="{{ path('businessprocesses_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i> </a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
