{% extends 'base.html.twig' %}

{% block body %}
    <h1>BusinessProcesses</h1>

     <table class="table table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ businessProcess.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ businessProcess.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a class="btn btn-default" href="{{ path('businessprocesses_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i> </a>
        </li>
        <li>
            <a class="btn btn-default" href="{{ path('businessprocesses_edit', { 'id': businessProcess.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
