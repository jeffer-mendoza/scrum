        {% extends 'layout.html.twig' %}

        {% block panel_title %}
            {{ sprint.id }} - {{ sprint.name }}
            <h6> Fecha Inicio: {% if sprint.startDate %}{{ sprint.startDate|date('d \\d\\e M \\d\\e Y') }}{% endif %}
                al {% if sprint.dueDate %}{{ sprint.dueDate|date('d \\d\\e M \\d\\e Y') }}{% endif %}
            </h6>
        {% endblock %}

        {% block panel_actions %}
            <a class="boton boton-default" href="{{ path('sprint_index') }}"><i class="fa fa-arrow-left" aria-hidden="true"></i>
            </a>
            <a class="boton boton-default" href="{{ path('sprint_edit', { 'id': sprint.id }) }}">Edit</a>
            <a class="boton boton-default" href="{{ path('report_sprint_backlog_pdf', {'id': sprint.id}) }}"><i
                        class="fa fa-file-pdf-o"></i> </a>
            <a class="boton boton-default" href="{{ path('report_sprint_backlog_image', {'id': sprint.id}) }}"><i
                        class="fa fa-file-image-o"></i> </a>
        {% endblock %}

         {% block th %}
             <th>Id</th>
             <th>Title</th>
             <th>Priority</th>
             <th>Points</th>
             <th>Status</th>
             <th>Effort</th>
         {% endblock %}

            {% block td %}
                {% for story in sprint.stories %}
                    <tr class="" title="{{ story.fullDescription }}">
                        <td>{{ story.id }}</td>
                        <td>{{ story.title }}</td>
                        <td>{{ story.strpriority }}</td>
                        <td>{{ story.points }}</td>
                        <td>{{ story.strstatus }}</td>
                        <td>{{ story.effort }}</td>
                        <td>
                            <a class="waves-effect waves-light btn" href="{{ path('story_show', { 'id': story.id }) }}"><i
                                        class="fa fa-eye"></i></a>
                            <a class="waves-effect waves-light btn" href="{{ path('story_edit', { 'id': story.id }) }}"><i
                                        class="fa fa-pencil"></i></a>
                        </td>
                    </tr>
                {% endfor %}
                {{ form_start(delete_form) }}
                <input class="boton boton-default" type="submit" value="Delete">
                {{ form_end(delete_form) }}
            {% endblock %}

           {% block footer %}
               <div id="chartContainer"></div>

               <script src="{{ asset('js/canvasjs.min.js') }}"></script>
               <script type="text/javascript">
                   window.onload = function () {
                       var chart = new CanvasJS.Chart("chartContainer", {
                           title: {
                               text: "Burndown"
                           },
                           axisX: {
                               interval: 10
                           },
                           data: [{
                               type: "line",
                               dataPoints: {{ array }}
        //                               [
        //                           {x: 10, y: 45},
        //                           {x: 20, y: 14},
        //                           {x: 30, y: 20},
        //                           {x: 40, y: 60},
        //                           {x: 50, y: 50},
        //                           {x: 60, y: 80},
        //                           {x: 70, y: 40},
        //                           {x: 80, y: 60},
        //                           {x: 90, y: 10},
        //                           {x: 100, y: 50},
        //                           {x: 110, y: 40},
        //                           {x: 120, y: 14},
        //                           {x: 130, y: 70},
        //                           {x: 140, y: 40},
//                           {x: 150, y: 90},
//                       ]
                   }]
               });
               chart.render();
           }
       </script>
   {% endblock %}
